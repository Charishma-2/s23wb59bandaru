extends layout

block content
  h1= title
  p Welcome to #{title}

  p Charishma Chowdary Bandaru
  
  button(type="button",onclick="doReduce()" class="btn btn-secondary") Press Me

  div(id="reduce") Reduce the value
  script.
    var count=20;
    var change=-1;
    function doReduce() {
      count = count+change;
      change--;
      document.getElementById("reduce").innerHTML=`Count ${count} with change ${change}`
    }
 

